{
	"name": "@discord-tickets/template",
	"version": "0.0.1",
	"type": "module",
	"main": "src/",
	"private": true,
	"scripts": {
		"build": "bun build --compile --minify --sourcemap --bytecode src --outfile service",
		"dev": "bun --hot src"
	},
	"devDependencies": {
		"@types/bun": "catalog:"
	},
	"peerDependencies": {
		"typescript": "catalog:"
	},
	"dependencies": {
		"@discord-tickets/logger": "workspace:*"
	}
}