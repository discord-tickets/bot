{
	"name": "@discord-tickets/interceptor",
	"version": "0.0.1",
	"type": "module",
	"module": "src/",
	"private": true,
	"scripts": {
		"build": "bun build --compile --minify --sourcemap --outfile interceptor src",
		"dev": "bun --hot src"
	},
	"devDependencies": {
		"@types/bun": "catalog:"
	},
	"peerDependencies": {
		"typescript": "catalog:"
	},
	"dependencies": {
		"@discord-tickets/logger": "workspace:*",
		"@discordeno/rest": "catalog:"
	}
}